[project]
name = "instamart-image-search"
version = "0.1.0"
description = "FastAPI app that classifies uploaded product images and searches Instamart."
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "google-genai",
    "python-multipart"
]

[tool.uv]
dev-dependencies = []
